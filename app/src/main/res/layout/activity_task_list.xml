<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@color/colorBackground"
    android:gravity="center">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_height="?attr/actionBarSize"
        android:layout_width="match_parent" >
    </android.support.v7.widget.Toolbar>
    <ListView
        android:id="@+id/task_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_horizontal_margin">
    </ListView>
    <View
        android:layout_width="fill_parent"
        android:layout_height="1px"
        android:background="@android:color/darker_gray"  />
    <ListView
        android:id="@+id/task_listComp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_horizontal_margin">
    </ListView>
    <Button
        android:layout_width="@dimen/plus_button_width"
        android:layout_height="@dimen/plus_button_height"
        android:background="@drawable/roundedbutton"
        android:text="@string/txt_plus_btn"
        android:textColor="@color/colorBackground"
        android:id="@+id/add_task"
        android:layout_gravity="right"
        android:layout_marginRight="@dimen/plus_button_margin_right"
        android:layout_marginBottom="@dimen/plus_button_margin_bottom"
        android:textSize="@dimen/plus_button_size" />
</LinearLayout>